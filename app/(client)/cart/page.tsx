import CartClientpage from "./CartClient";

// ✅ Add metadata export
export const metadata = {
  title: " MyCart",
  description: "Review your selected items and proceed to checkout.",
};

export default function CartPage() {
  return <CartClientpage />;
}
